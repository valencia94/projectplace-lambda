# ─── Runtime libraries ───────────────────────────────────────────
requests==2.31.0
boto3==1.34.89            # same major/minor as the current Lambda Python 3.10 runtime
pandas==1.3.5             # last pandas that ships wheels for Python 3.10 and is 100 % API-compatible with your code
numpy==1.23.5
openpyxl==3.0.10
python-docx==0.8.11
lxml==4.9.3

# ─── Lambda Runtime Interface Client (needed because we’re NOT using the
#      AWS “provided” base image) ─────────────────────────────────────────
awslambdaric==2.0.1
